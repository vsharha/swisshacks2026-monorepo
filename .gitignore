.DS_Store
**/.env
node_modules
dist
.idea

# Playwright MCP scratch (screenshots, snapshots)
.playwright-mcp/
/dashboard-*.png

# Runtime audit log (append-only write state, not seed data)
data/audit.jsonl

# Visual brainstorming companion
.superpowers/
